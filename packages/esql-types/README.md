# `@elastic/esql-types`

TypeScript type definitions for the ES|QL AST.

Pure type definitions — no runtime code, no dependencies.

## Install

```bash
npm install @elastic/esql-types
```

## Usage

```ts
import type { ESQLAstCommand, ESQLFunction } from '@elastic/esql-types';
```
