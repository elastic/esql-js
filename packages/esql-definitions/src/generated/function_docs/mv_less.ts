/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

// DO NOT MODIFY THIS FILE BY HAND. IT IS GENERATED BY
// `yarn workspace @elastic/esql-definitions generate` FROM
// `elasticsearch/esql/x-pack-esql/definition/functions/mv_less.json`.

import type { DefinitionDocs } from '../../definition_types';

const docs: DefinitionDocs = {
  description: 'Returns `true` if at least one value of `field` is less than `bound`, using the natural order of the type. A null or empty field returns `false`, as does a null or multivalued bound. The comparison is strict (`<`) by default; set `include_bound` to `true` in the optional `options` map to make it inclusive (`<=`). Works on any ordered type: numbers, dates, IPs, versions, and strings (compared by their UTF-8 bytes).',
  examples: [
    'ROW values = [1, 5, 10]\n| EVAL less = mv_less(values, 4)',
    'ROW values = [4]\n| EVAL less = mv_less(values, 4, {"include_bound": true})',
    'ROW words = ["apple", "cherry"]\n| EVAL less = mv_less(words, "banana")',
  ],
  params: {
    field: 'Multivalue expression to test. If null or empty, the function returns `false`.',
    bound: 'Comparison bound, of the same type as `field`. If null or multivalued, the function returns `false`.',
    options: '(Optional) Bound inclusivity options.',
  },
  markdown: '\n### MV LESS\nReturns `true` if at least one value of `field` is less than `bound`, using the natural order of the type. A null or empty field returns `false`, as does a null or multivalued bound. The comparison is strict (`<`) by default; set `include_bound` to `true` in the optional `options` map to make it inclusive (`<=`). Works on any ordered type: numbers, dates, IPs, versions, and strings (compared by their UTF-8 bytes).\n\n```esql\nROW values = [1, 5, 10]\n| EVAL less = mv_less(values, 4)\n```\n',
};

export default docs;
