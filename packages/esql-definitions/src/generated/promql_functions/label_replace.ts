/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

// DO NOT MODIFY THIS FILE BY HAND. IT IS GENERATED BY
// `yarn workspace @elastic/esql-definitions generate` FROM
// `elasticsearch/promql/x-pack-esql/definition/functions/label_replace.json`.

import type { PromqlFunctionDefinition } from '../../definition_types';

/**
 * Matches the regular expression `regex` against the value of the label `src_label`. On a match, sets the label `dst_label` to the expansion of `replacement`, substituting `$1`, `$name`, and `${name}` with the matched capture groups; on no match the input series is returned unchanged.
 *
 * @example
 * sum by (job2) (label_replace(http_requests_total, "job2", "$1", "job", "(.*)-server"))
 */
const definition: PromqlFunctionDefinition = {
  type: 'metadata',
  name: 'label_replace',
  signatures: [
    {
      params: [
        {
          name: 'v',
          type: 'instant_vector',
          optional: false,
          description: 'Instant vector input.',
        },
        {
          name: 'dst_label',
          type: 'scalar',
          optional: false,
          description: 'Name of the label to set.',
        },
        {
          name: 'replacement',
          type: 'scalar',
          optional: false,
          description: 'Replacement value, with `$1`/`$name`/`${name}` capture-group expansion.',
        },
        {
          name: 'src_label',
          type: 'scalar',
          optional: false,
          description: 'Name of the label to read.',
        },
        {
          name: 'regex',
          type: 'scalar',
          optional: false,
          description: 'Regular expression matched against `src_label`.',
        },
      ],
      variadic: false,
      returnType: 'instant_vector',
    },
  ],
  snapshotOnly: false,
};

export default definition;
