---
'@elastic/elasticsearch-query-builder': minor
'@elastic/elasticsearch-esql-dsl': minor
---

Move `@elastic/elasticsearch-query-builder` and `@elastic/elasticsearch-esql-dsl` from the `elasticsearch-dsl-js` repository into this monorepo. The packages keep their Apache-2.0 license and join the fixed release group, jumping to the shared monorepo version.
