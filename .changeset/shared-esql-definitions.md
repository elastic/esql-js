---
'@elastic/esql-definitions': minor
'@elastic/prismjs-esql': minor
---

Add `@elastic/esql-definitions` package with shared `commandsNames` and `functionNames` lists, and use it in `@elastic/prismjs-esql` instead of its own hardcoded lists.
