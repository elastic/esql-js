---
'@elastic/esql-definitions': patch
'@elastic/textmate-esql': patch
---

Add `DENSE_VECTOR` to `processingCommandNames` so it is highlighted as a command keyword in TextMate, Monaco, Prism.js, and Highlight.js grammars
